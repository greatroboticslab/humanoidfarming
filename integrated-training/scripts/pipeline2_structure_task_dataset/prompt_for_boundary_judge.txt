You are judging whether two adjacent subtask-groups should be merged into one coherent block.

Return ONLY JSON with:
{
  "decision": "merge" or "split",
  "reason": "short reason",
  "bridge": "optional 1 sentence that connects them if merge, else empty"
}

Rules:
- Merge if they share the same immediate goal, are sequential, and the right group depends on the left.
- Split if the topic/goal changes, a new phase begins, or narration is unrelated to the action.
- Prefer split when uncertain.
